#Использовать fs

Функция Download() Экспорт

	Сегменты = СтрРазделить(ЗапросHttp.Путь,"/",Ложь);
	
	Канал = Сегменты[0];
	Пакет = Сегменты[1];
	Файл  = Сегменты[2];

	ПутьКФайлу = ОбъединитьПути(ПереченьПакетов.КаталогХраненияПакетов(), Канал, Пакет, Файл);

	Если ФС.ФайлСуществует(ПутьКФайлу) Тогда
		Возврат Файл(ПутьКФайлу,"application/octet-stream",Файл);
	КонецЕсли;

	Возврат КодСостояния(404);

КонецФункции